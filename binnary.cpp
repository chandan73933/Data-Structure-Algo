#include<iostream>
using namespace std;
int main()
{
	int n,m,k,i,j,s;
	cin>>n>>m;
	int a[n],b[m],p[100]={};
	for(i=0;i<n;i++)
	cin>>a[i];
	for(j=0;j<m;j++)
	cin>>b[j];
		for(int p=0;p<k;p++)
		{
		if(a[i]&&b[j]==0)
		{
		
		cout<<(p[]=a[i]+b[j]);
		}
		else if(a[i]&&b[j]==1)
		{
		cout<<(p[]=a[i]+b[j]);
		}
		else
		{
		cout<<"not aa binary";
		}
		}
		
		
}
